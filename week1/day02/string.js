let batchName = "Playwright August" //literal declaration


//object dec
let browserName = new String("Chrome")
bName = "edge"
console.log(browserName.charAt(2))
console.log(browserName.indexOf('r'))

console.log("launch the browser" + " " + browserName)
console.log(`launch the browser` + " " + browserName)

console.log("launch the browser ${browserName}")
console.log(`launch the browser ${browserName}`)


let cName = "testleaf"
let reversedString = ""
for (let i=cName.length-1;i>=0;i--){
   reversedString+=cName.charAt(i)
}
console.log(reversedString)

let left = 0 
let temp=0 
let right=cName.length-1
for (let i=0;i<=cName.length-1;i++){
    temp = charAt(i)
    
}